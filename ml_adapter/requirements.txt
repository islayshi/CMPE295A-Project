# ML Adapter — Python dependencies
# Kept intentionally minimal to avoid polluting the Django backend's requirements.txt.
# TensorFlow and Vertex AI SDK will be added here in Month 2.

fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.8.2
httpx==0.27.2          # Used for testing (TestClient) and future Vertex AI calls

# Month 2 additions (uncomment when integrating Vertex AI):
# google-cloud-aiplatform==1.60.0
# tensorflow==2.17.0
